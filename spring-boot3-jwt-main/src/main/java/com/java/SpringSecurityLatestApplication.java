package com.java;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// This annotation indicates that this is the main application class and enables Spring Boot auto-configuration.
@SpringBootApplication
public class SpringSecurityLatestApplication {

	// The main method is the entry point of the application.
	public static void main(String[] args) {
		// This line starts the Spring Boot application.
		SpringApplication.run(SpringSecurityLatestApplication.class, args);
	}

}
